# trancolist

A script for the browser people to get a list of the top webpages.

## Usage

`python trancolist.py <out_dir>` to use default settings (ensure your email and API key are in `credentials.json`).

`python trancolist.py --help` for more usage information.

`python block.py [blocklist1.txt ...] <trancolist.txt>`
